# Simple Interest Calculator 🧮

This is a simple Bash script to calculate simple interest, built as a part of a Git and GitHub open source project assignment.

## 📌 What It Does

It calculates the simple interest using the standard formula:


The script prompts the user to enter:
- Principal amount
- Rate of interest
- Time (in years)

And then outputs the calculated simple interest.

## 🧑‍💻 How to Use

### 🔧 Prerequisites
Make sure you have a Bash terminal installed. On Windows, you can use Git Bash.

### ▶️ Run the script

```bash
bash simple-interest.sh
